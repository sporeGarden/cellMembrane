# Encrypted credential blobs
*.age
membrane-credentials.age

# DO API tokens
.doctl-token
token

# SSH keys (never commit)
*.pem
id_*
*.key

# Rust build artifacts (library-only workspace)
/target/
Cargo.lock

# Editor
*.swp
*.swo
*~
.DS_Store
